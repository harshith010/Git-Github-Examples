name: sch_Date
on:
    schedule:
    - cron: '*/2 * * * *'

jobs:
    hello_world:
        runs-on: ubuntu-latest
        steps:
         - name: hello
           run: echo "running schedule job ${date}"
